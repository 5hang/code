
悲观锁： 认为修改的时候别人会修改，，操作时候， 把数据锁主，别人无法访问   mutex

乐观锁： 认为我修改的时候，别人不会修改，再跟新的时候，会检测这时间是否有人在修改
          如果有人在该，它就会重试等操作 ，cas算法  atmic原子类操作

运用场景：
乐观锁：git push代码
数据库更新 增加一个字段 version=1 

悲观锁：select * from update

悲观锁开销 大于乐观锁

乐观锁使用场景： 写少，读多的操作
悲观锁： 临界区io操作 ，竞争激烈，并发多的操作
